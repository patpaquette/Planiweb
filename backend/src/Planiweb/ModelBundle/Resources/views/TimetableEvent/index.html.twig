{% extends '::base.html.twig' %}

{% block body -%}
    <h1>TimetableEvent list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Start_time</th>
                <th>End_time</th>
                <th>Day</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('timetable_event_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{% if entity.starttime %}{{ entity.starttime|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.endtime %}{{ entity.endtime|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.day }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('timetable_event_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('timetable_event_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('timetable_event_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
