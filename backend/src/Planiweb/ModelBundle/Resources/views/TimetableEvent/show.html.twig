{% extends '::base.html.twig' %}

{% block body -%}
    <h1>TimetableEvent</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Start_time</th>
                <td>{{ entity.starttime|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>End_time</th>
                <td>{{ entity.endtime|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Day</th>
                <td>{{ entity.day }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('timetable_event') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('timetable_event_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('timetable_event_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
{% endblock %}
