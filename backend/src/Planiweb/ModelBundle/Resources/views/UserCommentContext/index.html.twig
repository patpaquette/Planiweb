{% extends '::base.html.twig' %}

{% block body -%}
    <h1>UserCommentContext list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Context</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('user_comment_context_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.context }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('user_comment_context_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('user_comment_context_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('user_comment_context_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
