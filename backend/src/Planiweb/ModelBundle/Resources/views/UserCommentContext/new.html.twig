{% extends '::base.html.twig' %}

{% block body -%}
    <h1>UserCommentContext creation</h1>

    <form action="{{ path('user_comment_context_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('user_comment_context') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
